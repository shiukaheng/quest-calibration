class ClientMetadataContainer {
    // split into mandatory and
}

class BaseClient {
    constructor() {
        // BaseClient will have a base metadata object, classes that inherit BaseClient must also provide these metadata keys.
    }
    updateMetadata(key, value) {

    }
    

}